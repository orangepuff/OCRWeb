# OCRWeb.Frontend.Shared

Angular 22 shared component library for frontend-only reusable UI:
Text, Label, Button, CropPdf, and the popup/window/form primitives from the implementation plan.

> Placeholder. Not part of `OCRWeb.slnx` (Node/Angular toolchain, not MSBuild).
> Scope: frontend UI reuse only — no business rules here.

## Scaffold

```bash
# from repo root
ng generate library OCRWeb.Frontend.Shared
```
