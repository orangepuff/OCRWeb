/** Duration in milliseconds for MatSnackBar success toasts across the app. */
export const SNACK_DURATION_MS = 3000;

/**
 * Fraction of the canvas (0–1) that the default crop selection covers when a PDF is first
 * loaded. Applied symmetrically so the selection is centred on the page.
 */
export const DEFAULT_CROP_FILL = 0.85;
